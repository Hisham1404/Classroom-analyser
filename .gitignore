# Raw field data: real classroom audio, children's faces, teacher names, GPS.
# Never commit. See docs/data-notes.md §1.
data/

# Python
.cache/
.venv/
venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Model weights / derived media
*.mp3
*.wav
*.jpg
*.jpeg
*.png
# Anchored to the repo root ON PURPOSE. Unanchored, `models/` also matched
# app/src/app/models/, which silently kept session-result.ts out of the repo:
# every local build passed and every Vercel build failed on TS2307.
/models/

# Run logs
*.log

# Env
.env
.env.*
